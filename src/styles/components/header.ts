import styled from 'styled-components';

export const Container = styled.div`
  color: #f0f2f5;
  background: #9853ff;
  padding: 0 64px;

  a {
    color: #f0f2f5;
    font-size: 24px;
  }

  .content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 0 auto;
    max-width: 1024px;

    h1 {
      margin-left: auto;
    }
  }
`;
